#!/usr/bin/env node /** * Test script to verify broker review pages are working */ import fetch from 'node-fetch'; async function testBrokerPages() { console.log('🧪 Testing Broker Review Pages...'); console.log('================================='); const brokers = [ 'ig-group', 'oanda', 'xm-group', 'fxtm', 'pepperstone' ]; const baseUrl = 'http://localhost:5179'; for (const brokerSlug of brokers) { try { const url = `${baseUrl}/review/${brokerSlug}`; console.log(`\n🔍 Testing: ${url}`); const response = await fetch(url, { timeout: 5000, headers: { 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36' } }); if (response.ok) { const html = await response.text(); const has404 = html.includes('404') || html.includes('Page Not Found'); if (has404) { console.log(`❌ FAILED: ${brokerSlug} - Returning 404 page`); } else { console.log(`✅ SUCCESS: ${brokerSlug} - Page loaded successfully`); console.log(` Status: ${response.status} ${response.statusText}`); } } else { console.log(`❌ FAILED: ${brokerSlug} - HTTP ${response.status}`); } } catch (error) { console.log(`❌ ERROR: ${brokerSlug} - ${error.message}`); } } console.log('\n✅ Testing completed!'); } testBrokerPages().catch(console.error);