// Test file to verify environment variables are loaded console.log('Testing environment variables...'); console.log('VITE_SUPABASE_URL:', import.meta.env.VITE_SUPABASE_URL); console.log('VITE_SUPABASE_ANON_KEY:', import.meta.env.VITE_SUPABASE_ANON_KEY ? '***' : 'NOT FOUND'); console.log('BROKERS_DIR:', import.meta.env.BROKERS_DIR); // Test Supabase connection import { createClient } from '@supabase/supabase-js'; const supabaseUrl = import.meta.env.VITE_SUPABASE_URL; const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY; if (!supabaseUrl || !supabaseAnonKey) { console.error('Missing Supabase environment variables!'); } else { console.log('Environment variables loaded successfully'); try { const supabase = createClient(supabaseUrl, supabaseAnonKey); console.log('Supabase client created successfully'); // Test a simple query supabase.from('brokers').select('count', { count: 'exact', head: true }) .then(({ count, error }) => { if (error) { console.error('Error connecting to Supabase:', error); } else { console.log('Successfully connected to Supabase. Broker count:', count); } }); } catch (error) { console.error('Error creating Supabase client:', error); } }