import { useClerk } from '@clerk/clerk-react' import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog' import { Button } from '@/components/ui/button' interface ClerkAuthDialogProps { isOpen: boolean onClose: () => void } export function ClerkAuthDialog({ isOpen, onClose }: ClerkAuthDialogProps) { const { openSignIn } = useClerk() const currentPath = window.location.pathname + window.location.search return ( <Dialog open={isOpen} onOpenChange={onClose}> <DialogContent className="sm:max-w-md"> <DialogHeader> <DialogTitle>Welcome to BrokerAnalysis</DialogTitle> </DialogHeader> <div className="space-y-4"> <Button onClick={() => openSignIn({ redirectUrl: currentPath, signUpFallbackRedirectUrl: currentPath, afterSignInUrl: currentPath, afterSignUpUrl: currentPath })} className="w-full" > Continue with Email </Button> <div className="relative"> <div className="absolute inset-0 flex items-center"> <span className="w-full border-t" /> </div> <div className="relative flex justify-center text-xs uppercase"> <span className="bg-background px-2 text-muted-foreground"> Or continue with </span> </div> </div> <div className="grid grid-cols-2 gap-4"> <Button variant="outline" onClick={() => openSignIn({ redirectUrl: currentPath, signUpFallbackRedirectUrl: currentPath, afterSignInUrl: currentPath, afterSignUpUrl: currentPath })} > Google </Button> <Button variant="outline" onClick={() => openSignIn({ redirectUrl: currentPath, signUpFallbackRedirectUrl: currentPath, afterSignInUrl: currentPath, afterSignUpUrl: currentPath })} > Facebook </Button> </div> </div> </DialogContent> </Dialog> ) } 