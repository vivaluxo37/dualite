import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card' import { Check, Star, Shield, Zap, Globe, Users, Book } from 'lucide-react' interface FeatureItem { icon: React.ReactNode title: string description: string } interface BrokerFeaturesProps { features: FeatureItem[] className?: string } export function BrokerFeatures({ features, className }: BrokerFeaturesProps) { return ( <Card className={className}> <CardHeader> <CardTitle>Key Features & Benefits</CardTitle> </CardHeader> <CardContent> <div className="grid grid-cols-1 md:grid-cols-2 gap-4"> {features.map((feature, index) => ( <div key={index} className="flex items-start gap-3 p-3 rounded-lg border"> <div className="text-primary mt-1"> {feature.icon} </div> <div> <h3 className="font-semibold mb-1">{feature.title}</h3> <p className="text-sm text-muted-foreground">{feature.description}</p> </div> </div> ))} </div> </CardContent> </Card> ) } // Common feature icons for reuse export const FeatureIcons = { check: <Check className="w-5 h-5" />, star: <Star className="w-5 h-5" />, shield: <Shield className="w-5 h-5" />, zap: <Zap className="w-5 h-5" />, globe: <Globe className="w-5 h-5" />, users: <Users className="w-5 h-5" />, book: <Book className="w-5 h-5" />, }