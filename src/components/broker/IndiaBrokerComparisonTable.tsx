import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card' import { Badge } from '@/components/ui/badge' import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table' import { CheckCircle, XCircle } from 'lucide-react' interface IndiaBrokerComparisonTableProps { title: string headers: string[] rows: string[][] className?: string } export function IndiaBrokerComparisonTable({ title, headers, rows, className = "" }: IndiaBrokerComparisonTableProps) { return ( <Card className={className}> <CardHeader className="pb-4"> <CardTitle className="text-2xl md:text-3xl">{title}</CardTitle> </CardHeader> <CardContent className="p-0"> {/* Mobile-friendly responsive table */} <div className="block lg:hidden"> {rows.map((row, index) => ( <div key={index} className="border-b border-border p-4 last:border-b-0"> <h3 className="font-semibold text-lg mb-3">{row[0]}</h3> <div className="space-y-2"> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Regulators:</span> <span className="text-sm">{row[1]}</span> </div> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Year Established:</span> <span className="text-sm text-center">{row[2]}</span> </div> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Execution Type:</span> <span className="text-sm">{row[3]}</span> </div> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Min Deposit:</span> <span className="text-sm font-medium text-center">{row[4]}</span> </div> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Avg Cost EUR/USD:</span> <span className="text-sm font-medium text-center">{row[5]}</span> </div> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Avg Cost GBP/USD:</span> <span className="text-sm font-medium text-center">{row[6]}</span> </div> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Avg Cost Gold:</span> <span className="text-sm font-medium text-center">{row[7]}</span> </div> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Platforms:</span> <span className="text-sm">{row[8]}</span> </div> <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Islamic Account:</span> <div className="text-sm flex justify-center"> {row[9] === 'Yes' || row[9] === 'Available' ? ( <CheckCircle className="w-5 h-5 text-green-500" /> ) : ( <XCircle className="w-5 h-5 text-red-500" /> )} </div> </div> {row.length > 10 && ( <div className="grid grid-cols-2 gap-2"> <span className="text-sm text-muted-foreground">Negative Balance Protection:</span> <div className="text-sm flex justify-center"> {row[10] === 'Yes' || row[10] === 'Available' ? ( <CheckCircle className="w-5 h-5 text-green-500" /> ) : ( <XCircle className="w-5 h-5 text-red-500" /> )} </div> </div> )} </div> </div> ))} </div> {/* Desktop table */} <div className="hidden lg:block overflow-x-auto"> <Table> <TableHeader> <TableRow> {headers.map((header, index) => ( <TableHead key={index} className="font-semibold whitespace-nowrap"> {header} </TableHead> ))} </TableRow> </TableHeader> <TableBody> {rows.map((row, index) => ( <TableRow key={index} className="hover:bg-muted/50"> {row.map((cell, cellIndex) => ( <TableCell key={cellIndex} className={cellIndex >= 4 && cellIndex <= 7 ? "font-medium text-center" : ""}> {cellIndex === 9 || (cellIndex === 10 && row.length > 10) ? ( <div className="flex justify-center"> {cell === 'Yes' || cell === 'Available' ? ( <CheckCircle className="w-5 h-5 text-green-500 mx-auto" /> ) : cell === 'N/A' ? ( <span className="text-muted-foreground">N/A</span> ) : ( <XCircle className="w-5 h-5 text-red-500 mx-auto" /> )} </div> ) : ( cell )} </TableCell> ))} </TableRow> ))} </TableBody> </Table> </div> </CardContent> </Card> ) }